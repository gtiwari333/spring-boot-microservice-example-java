server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gtiwari333/spring-boot-microservice-example-java.git
          search-paths:
            - /configs/local
          default-label: master