eureka:
    client: # this is server, disable discovery and register this instance as client
        registerWithEureka: false
        serviceUrl:
            defaultZone: ${DISCOVERY_URL:http://localhost:8761}/eureka/
